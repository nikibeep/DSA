#include <stdio.h>
int main() {
int arr[5] = {10, 20, 30, 40, 50};
int target = 30;
int found = 0;
// Traverse array
for(int i = 0; i < 5; i++) {
// Check target
if(arr[i] == target) {
printf("Element found at index %d\n", i);
found = 1;
break;
}
}
if(found == 0) {
printf("Element not found\n");
5
}
return 0;
}
